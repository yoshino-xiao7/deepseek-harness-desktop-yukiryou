- insert:
    - id: desktop-settings
      name: '@dsh-desktop/settings'
    - id: desktop-companion
      name: '@dsh-desktop/companion'

# The rc.7 turn-tail is a first-match chain. The Companion entry reuses the
# exported official ProducedFiles component and appends its read-only card.
- id: ui-deliverables
  disabled: true
